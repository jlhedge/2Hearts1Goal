# Subscriptions Module

This module owns:

- plan catalog
- subscription state
- payment-provider orchestration
- webhook processing

Keep external providers such as Stripe or Braintree behind the infrastructure layer.
